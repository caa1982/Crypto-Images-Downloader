var config = {};

config.path = ""; // Change path (destination of the download)
config.resolution = "" // You can change the image resolution from 16x16, 32x32, 64x63 or 128x128

module.exports = config;