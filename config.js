var config = {};

// Change path (destination of the download)
config.path = ""; 

// You can change the image resolution from 16x16, 32x32, 64x64 or 128x128
config.resolution = ""; 

module.exports = config;